export interface ThemeColors {
  primary: string;
  primaryHover: string;
  secondary: string;
  success: string;
  warning: string;
  danger: string;
  info: string;
  background: string;
  surface: string;
  surfaceMuted: string;
  border: string;
  text: string;
  textMuted: string;
  textInverted: string;
}

export type ThemeMode = "light" | "dark";
export type ThemeName = "default" | "corporate" | "fintech" | "healthcare";
